# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:3000/api/v1

# MongoDB connection string (use a credentials/cluster URI)
checkMONGODB_URI=mongodb://localhost:27017/brainly
# JWT secret used to sign tokens (use a long random string)
JWT_SECRET=your_jwt_secret_here
# Add your backend API base URL here
